<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zeus.mapper.ItemMapper">
	<insert id="create" parameterType="Item">
		INSERT INTO item(id, name,
		price, description, url)
		VALUES(item_seq.NEXTVAL, #{name}, #{price},
		#{description}, #{url})
	</insert>

	<select id="read" resultType="Item">
		SELECT id, name, price,
		description, url FROM item WHERE id = #{id}
	</select>

	<update id="update">
		UPDATE item SET name = #{name}, price = #{price}, description = #{description}, url = #{url}
		WHERE id = #{id}
	</update>
	<delete id="delete">

	</delete>
	<select id="list" resultType="Item"> 
		<![CDATA[ 
			SELECT id, name, price, description, url FROM item 
			WHERE id > 0 ORDER BY id DESC 
		]]>
	</select>
	<select id="getPicture" resultType="String">
		SELECT url FROM item WHERE id
		= #{id}
	</select>
</mapper>